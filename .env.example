# Supabase Configuration
# Get these values from your Supabase project: Project Settings → API
# Do not commit real keys to version control.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# For vocab seed script (Admin → Vocab Lab seed)
# Get from Supabase: Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: enable debug logging for submit pipeline (browser console)
# localStorage.setItem('debug_submit', 'true')
